server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置Nacos地址
#        server-addr: localhost:8848
        server-addr: 192.168.31.137:1111 #换成nginx的1111端口做集群


management:
  endpoints:
    web:
      exposure:
        include: '*'
